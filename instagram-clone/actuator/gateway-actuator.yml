management:
    endpoints:
        web:
            exposure:
                include: health, gateway, info, gateway
    endpoint:
        health:
            show-details: always
        prometheus:
            enabled: true
    metrics:
        export:
          prometheus:
            enabled: true
