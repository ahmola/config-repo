spring:
  config:
    import:
      - optional:configserver:http://config-server:8888/common/consul-client.yaml
      - optional:configserver:http://config-server:8888/common/gateway-actuator.yaml
      - optioanl:configserver:http://config-server:8888/common/logging.yaml

  application:
    name: gateway

  autoconfigure:
      exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration

  cloud:
    gateway:
      server:
          webflux:
            discovery:
              locator:
                enabled: true
                lower-case-service-id: true

server:
  port: 8080